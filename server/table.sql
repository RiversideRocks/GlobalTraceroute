create database traceroute;
use traceroute;

CREATE TABLE traceroute (
    IP varchar(15),
    City varchar(255),
    Region varchar(255),
    Country varchar(255),
    Lat float(255),
    Lon float(255)
);